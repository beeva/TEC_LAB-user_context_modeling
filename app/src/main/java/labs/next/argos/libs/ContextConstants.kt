package labs.next.argos.libs

enum class ContextSource { USER, SENSOR }
enum class ContextState { WORK, STILL, TRANSIT, PERSONAL }
enum class ContextSensor { BATTERY, BLUETOOTH, LOCATION, NETWORK, USAGE, ACTIVITY, WIFI, MOVEMENT }